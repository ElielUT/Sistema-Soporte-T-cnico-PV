<%- include("templates/header.ejs") %>
    <h1>Antenas</h1>
    <div class="buscador">
        <form action="" id="formBusqueda" class="formBusqueda">
            <input type="text" name="Buscar" placeholder="Buscar" style="width: 100%;">
        </form>

        <button class="nuevo" id="nuevo"><b>+ Nuevo</b></button>
    </div>

    <%- include("templates/form_nuevo", { campos: "campos/antenas" }) %>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Dirección IP</th>
                <th>Ubicación</th>
                <th>Usuario / Contraseña</th>
                <th>Puerto</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(item=> { %>
                <tr id="<%= item.idantena %>" data-nombre="<%= item.nombre %>"
                    data-direccionip="<%= item.direccionip %>" data-ubicacion="<%= item.ubicacion %>"
                    data-puertoswitch="<%= item.puertoswitch %>" data-usuario="<%= item.usuario %>"
                    data-contrasena="<%= item.contrasena %>" data-idprod1="<%= item.idprod1 %>">
                    <td>
                        <%= item.idantena %>
                    </td>
                    <td>
                        <%= item.nombre %>
                    </td>
                    <td>
                        <%= item.direccionip %>
                    </td>
                    <td>
                        <%= item.ubicacion %>
                    </td>
                    <td>
                        <%= item.usuario %>
                            <%= item.contrasena %>
                    </td>
                    <td>
                        <%= item.puertoswitch %>
                    </td>
                    <%- include("templates/acciones", { subcategoria: item.subcategoria }) %>
                </tr>
                <% }) %>
        </tbody>
    </table>
    </div>

    <%- include("templates/form_editar", { campos: "campos/antenas" }) %>

        <script>
            window.paginaActual = "/antena"
        </script>
        <%- include("templates/footer.ejs") %>
