<%- include('templates/header_superior.ejs', {titulo: "Producto " + data.producto + " #" + data.idinvt ,
    pagAnterior: "/inventario" }) %>

    <% const marcas=[...new Set(data.items.map(item=> item.marca).filter(val => val && val.trim() !== ''))];
        const modelos = [...new Set(data.items.map(item => item.modelo).filter(val => val && val.trim() !== ''))];
        %>

        <div class="buscador">
            <form action="" id="formBusqueda" class="formBusqueda">
                <input type="text" name="Buscar" placeholder="Buscar">
                <%- include("templates/select", { nombre: "marca", placeholder: "Todas", opciones: marcas }) %>
                <%- include("templates/select", { nombre: "modelo", placeholder: "Todos", opciones: modelos }) %>
            </form>

            <button class="nuevo" id="nuevo"><b>+ Nuevo</b></button>
        </div>

        <%- include("templates/form_nuevo", { campos: "campos/producto" }) %>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Estado</th>
                    <th>Marca</th>
                    <th>Modelo</th>
                    <th>No. Serie</th>
                    <th>Cv. Licencia</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% data.items.forEach(item=> { %>
                    <tr id="<%= item.idprod %>" data-estado="<%= item.estado %>" data-marca="<%= item.marca %>"
                        data-modelo="<%= item.modelo %>" data-noserie="<%= item.noserie %>"
                        data-cvlicencia="<%= item.cvlicencia %>">
                        <td>
                            <%= item.idprod %>
                        </td>
                        <td>
                            <%= item.estado %>
                        </td>
                        <td>
                            <%= item.marca %>
                        </td>
                        <td>
                            <%= item.modelo %>
                        </td>
                        <td>
                            <%= item.noserie %>
                        </td>
                        <td>
                            <%= item.cvlicencia %>
                        </td>
                        <%- include("templates/acciones", { subcategoria: item.subcategoria }) %>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
        </div>

        <%- include("templates/form_editar", { campos: "campos/producto" }) %>

            <script>
                window.paginaActual = "/producto"
            </script>
            <%- include('templates/footer.ejs') %>
